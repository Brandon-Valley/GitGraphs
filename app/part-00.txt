<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#f4f0e8">
  <title>GitGraphs</title>
  <style>
    :root {
      --paper: #f4f0e8;
      --paper2: #ebe5da;
      --ink: #141414;
      --muted: #6d6a63;
      --line: #c9c1b5;
      --green: #20b981;
      --red: #e76660;
      --amber: #d89b2b;
      --dark: #101214;
      --radius: 20px;
    }

    * { box-sizing: border-box; }

    html {
      min-height: 100%;
      color: var(--ink);
      background: var(--paper);
      font-family: Inter, ui-sans-serif, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      -webkit-text-size-adjust: 100%;
    }

    body {
      min-height: 100vh;
      margin: 0;
      background:
        radial-gradient(circle at 16% 6%, rgba(255,255,255,.92), transparent 34rem),
        linear-gradient(180deg, var(--paper), #eee8de);
    }

    button, input { font: inherit; }
    button, a { -webkit-tap-highlight-color: transparent; }
    button { color: inherit; }

    .shell { min-height: 100vh; display: flex; flex-direction: column; }

    .header, .main, .footer {
      width: min(1160px, calc(100% - 30px));
      margin-left: auto;
      margin-right: auto;
    }

    .header {
      min-height: 72px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      border-bottom: 1px solid rgba(20,20,20,.12);
    }

    .brand {
      border: 0;
      padding: 8px 0;
      background: transparent;
      display: inline-flex;
      align-items: center;
      gap: 10px;
      cursor: pointer;
      font-size: 20px;
      font-weight: 900;
      letter-spacing: -.04em;
    }

    .brand-mark {
      width: 27px;
      height: 27px;
      display: inline-flex;
      align-items: flex-end;
      justify-content: center;
      gap: 3px;
    }

    .brand-mark i {
      display: block;
      width: 5px;
      border-radius: 2px;
      background: var(--ink);
    }

    .brand-mark i:nth-child(1) { height: 13px; }
    .brand-mark i:nth-child(2) { height: 21px; background: var(--green); }
    .brand-mark i:nth-child(3) { height: 10px; background: var(--red); }

    .badge {
      padding: 6px 9px;
      border-radius: 999px;
      background: var(--ink);
      color: white;
      font-size: 10px;
      font-weight: 900;
      letter-spacing: .08em;
    }

    .btn, .tab, .icon-btn {
      min-height: 42px;
      border: 1px solid var(--line);
      border-radius: 999px;
      background: rgba(255,255,255,.52);
      color: var(--ink);
      padding: 0 16px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      text-decoration: none;
      font-weight: 800;
      cursor: pointer;
    }

    .btn:hover, .tab:hover, .icon-btn:hover { background: rgba(255,255,255,.85); }
    .btn:active, .tab:active, .icon-btn:active { transform: translateY(1px); }
    .btn:disabled { opacity: .48; cursor: not-allowed; }
    .primary { background: var(--ink); border-color: var(--ink); color: white; }
    .primary:hover { background: #2a2a2a; }
    .icon-btn { width: 44px; padding: 0; }

    .home {
      padding: 78px 0 58px;
      text-align: center;
    }

    .eyebrow {
      color: var(--muted);
      font-size: 13px;
      font-weight: 850;
      letter-spacing: .10em;
      text-transform: uppercase;
    }

    .eyebrow:before {
      content: "";
      display: inline-block;
      width: 9px;
      height: 9px;
      margin-right: 10px;
      border-radius: 50%;
      background: var(--green);
      box-shadow: 0 0 0 6px rgba(32,185,129,.12);
      vertical-align: 1px;
    }

    h1 {
      max-width: 900px;
      margin: 28px auto 22px;
      font-size: clamp(48px, 8vw, 88px);
      line-height: .96;
      letter-spacing: -.065em;
    }

    .lead {
      max-width: 720px;
      margin: 0 auto 32px;
      color: var(--muted);
      font-size: clamp(17px, 2.2vw, 21px);
      line-height: 1.55;
    }

    .search {
      width: min(780px, 100%);
      margin: 0 auto;
      padding: 9px;
      display: grid;
      grid-template-columns: minmax(0,1fr) auto;
      gap: 8px;
      border: 1px solid rgba(20,20,20,.17);
      border-radius: 22px;
      background: rgba(255,255,255,.7);
      box-shadow: 0 18px 50px rgba(30,25,20,.1);
    }

    .field {
      min-width: 0;
      min-height: 54px;
      display: flex;
      align-items: center;
      overflow: hidden;
      border: 1px solid rgba(20,20,20,.08);
      border-radius: 15px;
      background: rgba(244,240,232,.75);
    }

    .field span { padding-left: 16px; color: var(--muted); font-weight: 750; white-space: nowrap; }
    .field input {
      min-width: 0;
      width: 100%;
      border: 0;
      outline: 0;
      padding: 0 14px 0 2px;
      background: transparent;
      color: var(--ink);
      font-size: 17px;
      font-weight: 800;
    }