t change</span><span>${b.n?(net>=0?"+":"")+fmt(net):"not loaded"}</span><span>Files changed</span><span>${b.n?fmt(b.f):"not loaded"}</span></div><div class="tr">${reps.map(([n,q])=>`<div><b>${esc(n)}</b><span>${q.n} commit${q.n===1?"":"s"}${q.a||q.d?` / +${fmt(q.a)} -${fmt(q.d)}`:""}</span></div>`).join("")}</div>`;E.tip.classList.add("on");const tw=E.tip.offsetWidth,th=E.tip.offsetHeight,m=8;let l=x+16;if(l+tw>r.width-m)l=x-tw-16;l=Math.max(m,Math.min(r.width-tw-m,l));let t=y-th-16;if(t<m)t=y+18;if(t+th>r.height-m)t=r.height-th-m;E.tip.style.left=l+"px";E.tip.style.top=Math.max(m,t)+"px"}
async function stats(){if(S.busy)return;const q=filtered().filter(c=>!c.stats).slice(0,35);if(!q.length)return toast("Line details are already loaded.");S.busy=true;E.stats.disabled=true;let done=0;load(true,"Loading exact line details 0 of "+q.length+"...",0);while(q.length){const batch=q.splice(0,3);await Promise.all(batch.map(async c=>{const [o,n]=repo(c).split("/");try{const d=await jsonp("/repos/"+encodeURIComponent(o)+"/"+encodeURIComponent(n)+"/commits/"+c.sha);c.stats={a:d.stats?.additions||0,d:d.stats?.deletions||0,f:d.files?.length||0}}catch(e){}done++;load(true,"Loading exact line details "+done+"...",done/(done+q.length)*100)}))}load(false);S.busy=false;E.stats.disabled=false;render();toast("Loaded exact line details for "+done+" commits.")}
$("form").onsubmit=e=>{e.preventDefault();user(E.user.value)};document.querySelectorAll(".ex").forEach(b=>b.onclick=()=>user(b.dataset.u));$("modes").onclick=e=>{const b=e.target.closest("[data-mode]");if(b){S.m=b.dataset.mode;render()}};$("ranges").onclick=e=>{const b=e.target.closest("[data-range]");if(b){S.r=b.dataset.range;render()}};E.stats.onclick=stats;$("refresh").onclick=()=>user(S.u);$("brand").onclick=home;E.back.onclick=hom