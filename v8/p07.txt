v){clean();e?no(e):ok(v)}window[cb]=x=>{const st=Number(x?.meta?.status||200);if(st<200||st>=300){const e=new Error(x?.data?.message||"GitHub returned "+st);e.status=st;end(e)}else end(null,x.data)};s.onerror=()=>end(new Error("GitHub request failed"));s.src=API+path+(path.includes("?")?"&":"?")+"callback="+cb+"&_="+Date.now();document.head.appendChild(s)})}
function norm(v){return String(v||"").trim().replace(/^https?:\/\/(www\.)?github\.com\//i,"").split(/[\/?#]/)[0]}function valid(v){return /^[A-Za-z0-9](?:[A-Za-z0-9-]{0,37}[A-Za-z0-9])?$/.test(v)}function uniq(a){const x=new Set;return a.filter(c=>{const k=repo(c)+":"+c.sha;if(x.has(k))return false;x.add(k);return true})}
async function user(raw){const u=norm(raw);if(!valid(u))return toast("Enter a valid GitHub username.",true);S.u=u;E.user.value=u;load(true,"Loading @"+u+"...",5);try{S.p=await jsonp("/users/"+encodeURIComponent(u));let all=[];for(let p=1;p<=3;p++){load(true,"Loading commit page "+p+" of up to 3...",15+p*23);const d=await jsonp("/search/commits?q="+encodeURIComponent("author:"+u)+"&sort=committer-date&order=desc&per_page=100&page="+p),a=(d.items||[]).filter(x=>!x.repository?.private);all.push(...a);if(a.length<100)break}S.c=uniq(all).sort((a,b)=>date(b)-date(a));show();render();location.hash=encodeURIComponent(u);load(false);if(!S.c.length)toast("No public commits were found.",true)}catch(e){load(false);toast(e.status===403?"GitHub public API limit reached. Try again later.":e.message||"GitHub request failed.",true)}}
function show(){E.home.classList.add("off");E.app.classList.add("on");E.back.hidden=false;E.avatar.src=S.p.avatar_url;E.name.textContent=S.p.name||S.p.login;E.meta.textContent=fmt(S.p.public_repos)+" public repositories / "+fmt(S.p.followers)+" followers";E.bio.textContent=S.p.bio||"Pu