:0,a:0,d:0});const q=b.r.get(rn);q.n++;if(c.stats){b.a+=c.stats.a;b.d+=c.stats.d;b.f+=c.stats.f;b.n++;q.a+=c.stats.a;q.d+=c.stats.d}}S.b=[...map.values()]}
function streak(a){const ds=[...new Set(a.map(c=>date(c).toISOString().slice(0,10)))].sort();let best=0,run=0,prev=0;for(const s of ds){const t=+new Date(s+"T00:00:00Z");run=prev&&t-prev===DAY?run+1:1;best=Math.max(best,run);prev=t}return best}
function render(){buckets();document.querySelectorAll("[data-mode]").forEach(b=>b.classList.toggle("on",b.dataset.mode===S.m));document.querySelectorAll("[data-range]").forEach(b=>b.classList.toggle("on",b.dataset.range===S.r));const ex={lines:"Green is lines added. Red is lines removed. Tap a period for repository details.",commits:"Each green bar is the number of commits in that period.",repos:"Each amber bar is the number of distinct repositories changed."};E.explain.textContent=ex[S.m];E.ct