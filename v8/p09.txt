.textContent={lines:"Lines added and removed",commits:"Commit count",repos:"Repositories changed"}[S.m];E.leg.textContent={lines:"green = added / red = removed",commits:"green = commits",repos:"amber = repositories"}[S.m];const a=filtered(),rs=new Map;let ad=0,de=0,kn=0;for(const c of a){rs.set(repo(c),(rs.get(repo(c))||0)+1);if(c.stats){ad+=c.stats.a;de+=c.stats.d;kn++}}$("mc").textContent=fmt(a.length);$("mr").textContent=fmt(rs.size);$("ma").textContent=kn?fmt(ad):"-";$("md").textContent=kn?fmt(de):"-";$("my").textContent=fmt(new Set(a.map(c=>date(c).toISOString().slice(0,10))).size);$("ms").textContent=fmt(streak(a));E.summary.textContent=fmt(a.length)+" commits across "+fmt(rs.size)+" repositories in "+S.r;E.notice.textContent=kn+" of "+a.length+" commits have exact line data";$("rr").textContent=S.r;E.cl.innerHTML=a.slice(0,13).map(c=>`<li class="row"><a href="${esc(c.html_url)}" target="_blank" rel="noopener"><div class="msg">${esc((c.commit?.message||"Commit").split("\n")[0])}</div><div class="sub"><span>${esc(repo(c))}</span><span>${esc(c.sha.slice(0,7))}</span><span>${date(c).toLocaleDateString()}</span></div></a></li>`).join("")||'<li class="row">No commits in this range.</li>';const top=[...rs].sort((a,b)=>b[1]-a[1]).slice(0,15),mx=top[0]?.[1]||1;E.rl.innerHTML=top.map(([r,n])=>`<li class="row"><div class="repo"><b>${esc(r)}</b><span>${fmt(n)}</span></div><div class="bar"><i style="width:${n/mx*100}%"></i></div></li>`).join("")||'<li class="row">No repositories.</li>';resize()}
function resize(){const r=E.chart.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);E.canvas.width=Math.round(r.width*d);E.canvas.height=Math.round(r.height*d);C.setTransform(d,0,0,d,0,0);draw()}
function draw(){const w=E.canvas.clientWidth,h=E.canvas.clientHeight,p={l:45,r:18