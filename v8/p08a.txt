blic commit activity across GitHub.";E.gh.href=S.p.html_url;scrollTo(0,0)}function home(){E.home.classList.remove("off");E.app.classList.remove("on");E.back.hidden=true;E.tip.classList.remove("on");history.replaceState(null,"",location.pathname+location.search)}
function start(){const n={"30D":30,"90D":90,"1Y":365}[S.r];return n?new Date(Date.now()-n*DAY):new Date(Math.min(...S.c.map(c=>+date(c)),Date.now()))}function filtered(){const s=+start();return S.c.filter(c=>+date(c)>=s)}
function buckets(){const a=filtered().slice().sort((x,y)=>date(x)-date(y));if(!a.length)return S.b=[];const st=+start(),en=Date.now(),step=Math.max(DAY,Math.ceil(Math.max(DAY,en-st)/42/DAY)*DAY),map=new Map;for(const c of a){const k=st+Math.floor((+date(c)-st)/step)*step;if(!map.has(k))map.set(k,{s:k,e:k+step,c:[],r:new Map,a:0,d:0,f:0,n:0});const b=map.get(k),rn=repo(c);b.c.push(c);if(!b.r.has(rn))b.r.set(rn,{n